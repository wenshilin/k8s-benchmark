apiVersion: v1
kind: ConfigMap
metadata:
  name: scheduler-1-config
  namespace: kube-system
data:
  config.yaml: |
    apiVersion: kubescheduler.config.k8s.io/v1alpha1
    kind: KubeSchedulerConfiguration
    schedulerName: linc-scheduler-ep
    algorithmSource:
      policy:
        configMap:
          namespace: kube-system
          name: scheduler-1-policy
    leaderElection:
      leaderElect: true
      lockObjectName: linc-scheduler-ep
      lockObjectNamespace: kube-system
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: scheduler-1-policy
  namespace: kube-system
data:
 policy.cfg : |
  {
    "kind" : "Policy",
    "apiVersion" : "v1",
    "predicates" : [
      {"name" : "MatchInterPodAffinity"},
      {"name" : "CheckVolumeBinding"},
      {"name" : "GeneralPredicates"},
      {"name" : "NoDiskConflict"},
      {"name" : "PodToleratesNodeTaints"},
      {"name" : "NoVolumeZoneConflict"}
    ],
    "priorities" : [
      {"name" : "EqualPriority", "weight" : 1}
    ],
    "extenders" : []
  }
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: linc-scheduler-ep
  namespace: kube-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app: linc-scheduler-ep
  template:
    metadata:
      labels:
        app: linc-scheduler-ep
    spec:
      nodeSelector:
        node-role.kubernetes.io/master: ""
      tolerations:
        - key: "node-role.kubernetes.io/master"
          operator: "Equal"
          value: ""
          effect: "NoSchedule"
      serviceAccountName: my-scheduler
      volumes:
      - name: scheduler-1-config
        configMap:
          name: scheduler-1-config
      containers:
      - name: scheduler-1
        image: 10.1.114.59:5000/linc-scheduler:v1.0
        command:
          - kube-scheduler
          - --config=/scheduler/config.yaml
        volumeMounts:
        - name: scheduler-1-config
          mountPath: /scheduler
        resources:
          limits:
            memory: "128Mi"
            cpu: "100m"
